import 'package:flutter/material.dart';

/// App-local color palette. Add new colors as `static const` — no ThemeExtension boilerplate.
final class PlaygroundColors {
  static const chartSignal = Color(0xFFE53935);
  static const chartLower = Color(0xFF43A047);
  static const chartUpper = Color(0xFFFB8C00);
  static const chartGrid = Color(0x1AFFFFFF);
}
