import 'package:flutter_supper_app_core/core.dart';

/// Central theme config for Vulcan Playground — override core defaults here.
final class PlaygroundThemeConfig {
  // ── Predefined Core Config ──
  static const config = AppThemeConfig(
    // Override core colors when needed:
    // lightColors: AppColors.light.copyWith(primary: Color(0xFF...)),
    // darkColors: AppColors.dark.copyWith(primary: Color(0xFF...)),
  );
}
